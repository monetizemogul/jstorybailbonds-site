/* Interactive FAQ, calculator, and assistant types */
export interface FAQItem {
  question: string;
  answer: string;
}

export interface BailStep {
  title: string;
  description: string;
  icon: string;
}
